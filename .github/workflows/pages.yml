# Publica el panel en GitHub Pages en cada push a main.
#
# Para activarlo, una sola vez: Settings → Pages → Source: GitHub Actions.
# Queda en https://<usuario>.github.io/<repositorio>/
name: Publicar en GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Si llegan varios push seguidos, basta con publicar el último.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # El sitio cuelga de /<repositorio>/, así que las rutas no pueden ser absolutas.
      - run: npx vite build --base="/${GITHUB_REPOSITORY#*/}/"

      # Pages sirve ficheros estáticos: sin este 404 la recarga rompería si algún
      # día se añaden rutas. Copiar el index cubre ese caso.
      - run: cp dist/index.html dist/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
